import { LetterValidatorDirective } from './letter-validator.directive';

describe('LetterValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
